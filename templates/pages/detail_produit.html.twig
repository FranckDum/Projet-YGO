{# {% extends 'base.html.twig' %}

{% block title %}Détails du produit{% endblock %}

{% block body %}

    <h1>Détails du produit : {{ produit.nomProduit }}</h1>

                            {% for carte in apiProduits %}

                                {% if carte.id == produit.ygoId %}

                <div class="d-flex flex-row">
                    <div class="col-md-3 col-lg-4 d-flex flex-column align-items-center border border-3 border-opacity-50 border-black mx-3 my-3 rounded bg-secondary carte">
                        
                        <div>
                                    <img class="img-fluid" src="/Images/ImgCartes/normal/{{carte.id}}.jpg">
                        </div>
                    </div>

                    <div class="col-md-6 col-lg-8 d-flex flex-column align-items-center border border-3 border-opacity-50 border-black mx-3 my-3 rounded bg-secondary carte">
                            <div class="flex-row">
                                <h2 class="text-center"> {{ produit.nomProduit }} </h2>
                                <h3 class="mt-5">Description :</h3>
                                <div class="mt-3 lead">
                                    {{ carte.desc }}
                                </div>
                                <div class="mt-5 lead">
                                    Stock : {{ produit.stock }}
                                </div>
                                <div class="mt-5 lead">
                                    {{ produit.prix|number_format(2) }} €
                                </div>
                            </div>
                    </div>
                </div>

        {% else %}
        <p>Aucun produit trouvé.</p>
        {% endif %}

        {% endfor %}

{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Détails du produit{% endblock %}

{% block h1 %}Détails du produit : {{ produit.nomProduit }}{% endblock %}

{% block body %}

    {# {% for carte in apiProduits %}
        {% if carte.id == produit.ygoId %}
            <div class="container">
                <div class= "row">
                    <div class="col-md-3 col-lg-4 d-flex flex-column align-items-center border border-3 border-opacity-50 border-black mx-3 my-3 rounded bg-secondary ms-auto me-auto carte">
                    <h2 class="text-center h2Produit"> {{ produit.nomProduit }} </h2>
                        <img class="img-fluid" src="/Images/ImgCartes/normal/{{carte.id}}.jpg">
                    </div>
                    <div class="flex-row w-auto col-md-6 bg-secondary rounded">
                        <h3 class="mt-5">Description :</h3>
                        <div class="mt-3 lead">
                            {{ carte.desc }}
                        </div>
                        <div class="mt-5 lead">
                            Stock : {{ produit.stock }}
                        </div>
                        <div class="mt-5 lead">
                            Prix : {{ produit.prix|number_format(2) }} €
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
    {% endfor %} #}

{# //Test// #}
    {# {% for carte in apiProduits %} 
    {% if carte.id == produit.ygoId %}
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h2 class="text-center h2Produit">{{ produit.nomProduit }}</h2>
                    <img class="img-fluid" src="/Images/ImgCartes/normal/{{ carte.id }}.jpg">
                </div>
                <div class="col-md-8 mt-auto mb-auto">
                    <div class="bg-secondary rounded p-3">
                        <h3 class="mt-3">Description :</h3>
                        <div class="mt-3 lead">{{ carte.desc }}</div>
                        <div class="mt-3 lead">Stock : {{ produit.stock }}</div>
                        <div class="mt-3 lead">Prix : {{ produit.prix|number_format(2) }} €</div>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endfor %} #}

{# //Test 2// #}
{% for carte in apiProduits %} 
    {% if carte.id == produit.ygoId %}
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="bg-secondary rounded p-3">
                        <div class="row">
                            <div class="col-md-4">
                                <h2 class="text-center h2Produit">{{ produit.nomProduit }}</h2>
                                <img class="img-fluid" src="/Images/ImgCartes/normal/{{ carte.id }}.jpg">
                            </div>
                            <div class="col-md-8">
                                <h3 class="mt-3">Description :</h3>
                                <div class="mt-3 lead">{{ carte.desc }}</div>
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mt-3 lead">Stock : {{ produit.stock }}</div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="mt-3 lead">Prix : {{ produit.prix|number_format(2) }} €</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endfor %}



{% if produitsSimilaires %}

            <div class="row d-flex justify-content-center flex-wrap mt-5">
            <h3 class="text-center">Vous aimerez peut-être :</h3>
                {% for produitSimilaire in produitsSimilaires %}
                    <div class="col-12 col-md-4 col-lg-3 d-flex flex-column align-items-center border border-3 border-opacity-50 border-black mx-3 my-3 rounded bg-secondary carte">
                        <a class="a" href="{{ path('detailProduit', {'id': produitSimilaire.id}) }}">
                            <h4 class="text-center">{{ produitSimilaire.nomProduit }}</h4>
                        </a>
                        <a class="a" href="{{ path('detailProduit', {'id': produitSimilaire.id}) }}">
                            <div class="mt-auto">
                            <img class="img-fluid mt-auto" src="/Images/ImgCartes/normal/{{ produitSimilaire.getYgoId() }}.jpg">
                            </div>
                        </a>
                        <div>
                            {# <p class="card-text">Description : {{ produitSimilaire.desc }}</p> #}
                            <p class="card-text">Stock : {{ produitSimilaire.stock }}</p>
                            <p class="card-text">Prix : {{ produitSimilaire.prix|number_format(2) }} €</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
{% endif %}
{% endblock %}

